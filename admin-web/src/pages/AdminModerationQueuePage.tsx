/**
 * Screen 31 — Admin Moderation Queue (docs/04-screen-list.md §31).
 * Placeholder only: real queue/decision UI arrives in a later module.
 */
export function AdminModerationQueuePage() {
  return (
    <div className="page">
      <h1>Hàng đợi Kiểm duyệt</h1>
      <p>Admin Moderation Queue — placeholder screen.</p>
    </div>
  )
}
