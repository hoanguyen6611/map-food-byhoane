/**
 * Screen 33 — Admin User Management (docs/04-screen-list.md §33).
 * Placeholder only: real user table/actions arrive in a later module.
 */
export function AdminUserManagementPage() {
  return (
    <div className="page">
      <h1>Quản lý Người dùng</h1>
      <p>Admin User Management — placeholder screen.</p>
    </div>
  )
}
