import { Module } from '@nestjs/common';

// Implemented by docs/build-prompts/06-reviews-scoring.md — review CRUD,
// criteria ratings, composite-score recompute job.
@Module({})
export class ReviewModule {}
